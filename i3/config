# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Basic options {{{
set $mod Mod1
set $modSuper Mod4

# font for window titles. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font xft:Inconsolata Medium 1

# xboxVLC
# exec /usr/bin/python2.7 ~/org/software/python/xboxControllerVLC/src/xboxVLC.py &

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# load .bashrc
exec (source ~/.bashrc)&
exec (udiskie --tray)&
exec (~/.dropbox-dist/dropboxd)&

# back and forth switching
# workspace_auto_back_and_forth yes

# }}}
# Mappings {{{

# | Keys                | command                              |
# |---------------------+--------------------------------------|
# | mod+1               | emacs_main                           |
# | mod+2               | Google-chrome                        |
# | mod+3               | vim                                  |
# | mod+4               | workspace 4: term                    |
# | mod+a               | workspace a: alfa                    |
# | mod+b               | backward console                     |
# | mod+c               | exec amixer -q set Master 5%-        |
# | mod+d               | workspace d: gamma                   |
# | mod+Down            | focus down                           |
# | mod+e               | moc scratchpad                       |
# | mod+f               | forward console                      |
# | mod+g               | focus parent                         |
# | mod+h               | focus left                           |
# | mod+i               | split v                              |
# | mod+j               | focus down                           |
# | mod+k               | focus up                             |
# | mod+l               | focus right                          |
# | mod+m               | mode resize                          |
# | mod+n               | layout default                       |
# | mod+Left            | focus left                           |
# | mod+o               | fullscreen                           |
# | mod+p               | exec dmenu_run                       |
# | mod+q               | ranger scratchpad                    |
# | mod+r               | workspace r: video                   |
# | mod+s               | workspace s: beta                    |
# | mod+t               | layout tabbed                        |
# | mod+u               | split h                              |
# | mod+v               | exec amixer -q set Master 5%+        |
# | mod+w               | workspace w: email                   |
# | mod+x               | emacs M-x                            |
# | mod+y               | scratchpad                           |
# | mod+z               | urxvtcd scratchpad                   |
# | mod+<               | Emacs_scratchpad                     |
# | mod+space           | focus mode_toggle                    |
# | mod+Up              | focus up                             |
# | mod+Right           | focus right                          |
# | mod+Shift+1         | move container to workspace 1: emacs |
# | mod+Shift+2         | move container to workspace 2: www   |
# | mod+Shift+3         | move container to workspace 3: dev   |
# | mod+Shift+4         | move container to workspace 4: term  |
# | mod+Shift+a         | move container to workspace a: alfa  |
# | mod+Shift+C         | kill                                 |
# | mod+Shift+d         | move container to workspace d: gamma |
# | mod+Shift+Down      | move down                            |
# | mod+Shift+e         | move container to workspace e: music |
# | mod+Shift+H         | move left                            |
# | mod+Shift+i         | reload                               |
# | mod+Shift+J         | move down                            |
# | mod+Shift+K         | move up                              |
# | mod+Shift+L         | move right                           |
# | mod+Shift+Left      | move left                            |
# | mod+Shift+q         | move container to workspace q: files |
# | mod+Shift+r         | move container to workspace r: video |
# | mod+Shift+Return    | exec urxvtcd                         |
# | mod+Shift+Right     | move right                           |
# | mod+Shift+s         | move container to workspace s: beta  |
# | mod+Shift+space     | floating toggle                      |
# | mod+Shift+u         | restart                              |
# | mod+Shift+Up        | move up                              |
# | mod+Shift+w         | move container to workspace w: email |
# | mod+Shift+Z         | exit                                 |
# | mod+Shift+y         | move scratchpad                      |
# | mod+Control+Shift+h | resize shrink width 10 px or 10 ppt  |
# | mod+Control+Shift+j | resize grow height 10 px or 10 ppt   |
# | mod+Control+Shift+k | resize shrink height 10 px or 10 ppt |
# | mod+Control+Shift+l | resize grow width 10 px or 10 ppt    |
#
# individual_free_keys = [i,n,y]


# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Shift+Return exec urxvtcd

# Volume shortcuts
bindsym $mod+v exec amixer -q set Master 5%+
bindsym $mod+c exec amixer -q set Master 5%-

# kill focused window
bindsym $mod+Shift+C kill

# start dmenu (a program launcher)
bindsym $mod+p exec dmenu_run

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+i split h

# split in vertical orientation
bindsym $mod+u split v

# enter fullscreen mode for the focused container
bindsym $mod+o fullscreen

# change container layout (stacked, tabbed, default)
# bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
# bindsym $mod+n layout stacked
bindsym $mod+n layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+g focus parent


# switch to workspace
# bindsym $mod+1 workspace 1: emacs
# bindsym $mod+2 workspace 2: www
bindsym $mod+3 workspace 3: dev
bindsym $mod+4 workspace 4: term
# bindsym $mod+q workspace q: files
# bindsym $mod+w workspace w: email
# bindsym $mod+e workspace e: music
bindsym $mod+r workspace r: video
bindsym $mod+a workspace a: alfa
bindsym $mod+s workspace s: beta
bindsym $mod+d workspace d: gamma
bindsym $mod+y scratchpad show

# assign workspace to output
workspace "1: emacs" output DVI-D-0
workspace "3: dev" output DVI-D-0
workspace "4: term" output DVI-D-0
workspace "a: alfa" output DVI-D-0
workspace "s: beta" output DVI-D-0

workspace "2: www" output DVI-I-1
workspace "q: files" output DVI-I-1
workspace "e: music" output DVI-I-1
workspace "w: email" output DVI-I-1
workspace "r: video" output DVI-I-1
workspace "d: gamma" output DVI-I-1

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1: emacs
bindsym $mod+Shift+2 move container to workspace 2: www
bindsym $mod+Shift+3 move container to workspace 3: dev
bindsym $mod+Shift+4 move container to workspace 4: term
bindsym $mod+Shift+q move container to workspace q: files
bindsym $mod+Shift+w move container to workspace w: email
bindsym $mod+Shift+e move container to workspace e: music
bindsym $mod+Shift+r move container to workspace r: video
bindsym $mod+Shift+a move container to workspace a: alfa
bindsym $mod+Shift+s move container to workspace s: beta
bindsym $mod+Shift+d move container to workspace d: gamma
bindsym $mod+Shift+y move scratchpad

# move workspace to another screen
bindsym $mod+Control+Shift+h move workspace to output DVI-D-0
bindsym $mod+Control+Shift+l move workspace to output DVI-I-1

# move scratchpad
bindsym $modSuper+h move container to output DVI-D-0
bindsym $modSuper+l move container to output DVI-I-1

# like $mod+y but undoing all the changes
bindsym $modSuper+u floating toggle; move scratchpad

bindsym $modSuper+Shift+h move container to output DVI-D-0; focus output left; focus floating
bindsym $modSuper+Shift+l move container to output DVI-I-1; focus output right; focus floating

# reload the configuration file
bindsym $mod+Shift+i reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+u restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+Z exit


# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+m mode "resize"
# }}}
# Applications {{{

assign [instance="^Emacs_main$"] 1: emacs
for_window [instance="^Emacs_main$"] 1: emacs

assign [class="^Google-chrome$"] 2: www
for_window [class="^Google-chrome$"] 2: www

assign [instance="^vim$"] 3: dev
for_window [instance="^vim$"] 3: dev

# assign [instance="^ranger$"] q: files
for_window [instance="^ranger$"] move scratchpad

# assign [instance="^MOC$"] e: music
for_window [title="^MOC$"] move scratchpad

assign [class="^mutt$"] w: email
for_window [class="^mutt$"] w: email

assign [class="^Vlc$"] r: video
for_window [class="^Vlc$"] r: video

# Applications bindings
for_window [instance="^scratchpad$"] move scratchpad
for_window [instance="^Emacs_scratchpad"] move scratchpad

bindsym $mod+1 [instance="^Emacs_main$"] focus
bindsym $mod+2 [class="^Google-chrome$"] focus
bindsym $mod+q [instance="^ranger$"] scratchpad show
bindsym $mod+e [title="^MOC.*$"] scratchpad show
bindsym $mod+w [instance="^mutt$"] focus
bindsym $mod+z [instance="^scratchpad$"] scratchpad show
bindsym $mod+less [instance="^Emacs_scratchpad$"] scratchpad show

# # }}}
# Appearance {{{

# Change border style
new_window 1pixel
# new_window pixel 2

# |colourclass|border|bckgrnd|text|
# Green
client.focused #7cfc00 #7cfc00 #000000
# client.focused #4169e1 #00ffff #7cfc00
client.focused_inactive #7cfc00 #000000 #7cfc00

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    position top
    mode invisible
    hidden_state hide
    colors {
        background #555555
        statusline #7cfc00
        # separator #7cfc00
        focused_workspace #000000 #00cc99 #000000
        active_workspace #000000 #000000 #FFFFFF
    }
    status_command i3status -c ~/.i3/i3status.conf
}

# screen configuration
exec xrandr --output DVI-I-1 --auto --right-of DVI-D-0
exec xrandr --output DVI-D-0 --primary

# change my background
# exec feh --bg-scale /home/rayenok/img/wallpapers/ciudad\ noche/city.jpg
# exec feh --bg-scale /home/rayenok/img/hex_blue.jpg
# exec feh --bg-scale --no-xinerama /home/rayenok/img/stars.jpg
exec feh --bg-scale --no-xinerama /home/rayenok/img/world.jpg
# exec DISPLAY=0.0 feh --bg-scale /home/rayenok/img/stars-1.jpg
# exec DISPLAY=0.1 feh --bg-scale /home/rayenok/img/stars-2.jpg

# Google music
exec google-musicmanager

#}}}
# Applications execution {{{

exec --no-startup-id i3-msg 'workspace 1: emacs; exec (emacs -name "Emacs_main")&'
exec --no-startup-id i3-msg 'workspace 2: www; exec google-chrome'
exec --no-startup-id i3-msg 'workspace 3; exec (terminator -c "vim" -e "vim --servername vim_i3")& '
exec --no-startup-id i3-msg 'workspace 4: term; exec urxvtcd'
exec --no-startup-id i3-msg 'workspace w: email; exec (urxvtcd -name "mutt" -e mutt)&'
exec --no-startup-id urxvtcd -name "ranger" -fn "7x13" -e ranger
exec --no-startup-id urxvtcd -T "MOC" -e mocp
exec --no-startup-id urxvtcd -name "scratchpad" -e bash -c "tmux attach -t scratchpad || tmux new-session -s scratchpad"
exec --no-startup-id emacs -name "Emacs_scratchpad" -g 134x57+483+136 --no-splash --funcall "org-agenda-list"

#
